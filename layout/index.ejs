<header>
    <h1 class="indexTitle"><%- theme.subtitle_main %></h1>
    <p class="indexSubtitle"><%- theme.susbtitle_secondary %></p>
</header>

<section class="tiles">
    <% page.posts.forEach(function(item, index){ %>
        <%- partial('_partial/article-index', {item: item, index: index}) %>
    <% }); %>
</section>

<%- partial('_partial/pagination') %>
